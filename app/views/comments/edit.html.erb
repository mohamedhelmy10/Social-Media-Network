<%= form_with model: [@user ,@post, @comment, @user.posts.find(@post.id).comments.find(@comment.id).build ] do |form| %>
    <p>
      <%= form.label :comment %><br>
      <%= form.text_area :body %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>